<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Font Scale Calculator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #121212;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .calculator {
            max-width: 400px;
            width: 90%;
            padding: 20px;
            border: 1px solid #333;
            border-radius: 10px;
            background-color: #1e1e1e;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
        }
        .calculator h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #4caf50;
        }
        .calculator input, .calculator button {
            width: 100%;
            margin-bottom: 15px;
            padding: 10px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
        }
        .calculator input {
            background-color: #333;
            color: #ffffff;
        }
        .calculator button {
            background-color: #2196f3;
            color: #ffffff;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .calculator button:hover {
            background-color: #1e88e5;
        }
        .output {
            margin-top: 20px;
        }
        .output div {
            margin-bottom: 20px;
            background-color: #2c2c2c;
            padding: 10px;
            border-radius: 5px;
        }
        .output div strong {
            color: #4caf50;
        }
        .description {
            margin-bottom: 20px;
            font-size: 14px;
            color: #b0bec5;
        }
    </style>
</head>
<body>
    <div class="calculator">
        <h2>Font Scale Calculator</h2>
        <div class="description">
            Enter a base font size to calculate harmonious font sizes using these scales:
            <ul>
                <li><strong>Perfect Fourth (1.333):</strong> Balanced and versatile for modern designs.</li>
                <li><strong>Golden Ratio (1.618):</strong> Aesthetic and dramatic, ideal for impactful typography.</li>
                <li><strong>Major Third (1.25):</strong> Subtle and clean, perfect for minimalist layouts.</li>
                <li><strong>Double (2):</strong> Bold and contrasting, suitable for headlines and CTAs.</li>
            </ul>
        </div>
        <label for="baseFontSize">Enter Base Font Size (px):</label>
        <input type="number" id="baseFontSize" placeholder="e.g., 16" />
        <button onclick="calculateScales()">Calculate</button>
        <div class="output" id="output"></div>
    </div>

    <script>
        function calculateScales() {
            const baseSize = parseFloat(document.getElementById('baseFontSize').value);
            const output = document.getElementById('output');
            output.innerHTML = '';

            if (isNaN(baseSize) || baseSize <= 0) {
                output.innerHTML = '<div>Please enter a valid font size.</div>';
                return;
            }

            const scales = {
                'Perfect Fourth (1.333)': 1.333,
                'Golden Ratio (1.618)': 1.618,
                'Major Third (1.25)': 1.25,
                'Double (2)': 2
            };

            for (const [name, ratio] of Object.entries(scales)) {
                const larger = (baseSize * ratio).toFixed(2);
                const smaller = (baseSize / ratio).toFixed(2);
                output.innerHTML += `
                    <div>
                        <strong>${name}:</strong><br />
                        Larger: ${larger}px<br />
                        Smaller: ${smaller}px
                    </div>
                `;
            }
        }
    </script>
</body>
</html>
